VITE_PRIVY_APP_ID=cm-your-privy-app-id

# Optional: Cloudflare Worker endpoint for forwarding logs/toasts to a Telegram chat.
# Leave empty to disable remote logging. Do NOT include trailing /log (code appends it).
# VITE_LOG_ENDPOINT=https://privy-tg-test-worker.<your-subdomain>.workers.dev

